# Auto-reload the server whenever source files change so deployments pick up updates
reload = True
# Keep at least one worker; additional configuration can be set via environment if needed
workers = 1
